apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: posts-app
  namespace: argocd
spec:
  project: default  
  source:
    repoURL: docker.io/noabl
    targetRevision: 0.1.2
    chart: posts-app-chart
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true